name: Build

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📦  Checkout
      uses: actions/checkout@v2
    - name: 🐥. Setup node
      uses: actions/setup-node@v2
      with:
        node-version: '14'
    - run: yarn
    - run: yarn build
